import 'package:flutter/material.dart';

class LabledText extends StatelessWidget {
  const LabledText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
